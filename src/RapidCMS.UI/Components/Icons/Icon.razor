<i class="ms-Icon ms-Icon--@Name @(string.IsNullOrWhiteSpace(Color) ? "" : $"ms-fontColor-shared{Color}")" aria-hidden="true"></i>

@code {
    [Parameter] public string? Name { get; set; }
    [Parameter] public string? Color { get; set; }
}