@inherits BaseDisplay

<pre class="col-form-label">@value</pre>

@code {
    private string? value { get; set; }

    protected override void OnParametersSet()
    {
        value = @GetValueAsString();

        if (string.IsNullOrWhiteSpace(value))
        {
            value = " ";
        }
    }
} 