@*
    The Router component displays whichever component has a @page
    directive matching the current URI.
*@

@using RapidCMS.UI.Components.Pages


@*<Router AppAssembly="typeof(RapidCMS.UI.Startup).Assembly" />*@

<RapidCmsRouter CustomButton1Alias="create-button">
        <CustomButton1>
            <CreateButton TContext="BasePage" />
        </CustomButton1>
    </RapidCmsRouter>